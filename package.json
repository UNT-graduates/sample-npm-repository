{
  "name": "sample-npm-repository",
  "version": "1.0.0",
  "description": "Sample NPM repository",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "echo \"Building project...\" && mkdir -p dist && echo \"Build complete\" > dist/build.txt"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@angular/cli": "^12.0.0",
    "@angular/core": "^12.0.0"
  },
  "devDependencies": {
    "typescript": "^4.2.0"
  }
}
